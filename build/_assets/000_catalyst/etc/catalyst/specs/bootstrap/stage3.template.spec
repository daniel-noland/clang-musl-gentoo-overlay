subarch: ${ARCH}
target: stage2
version_stamp: ${VERSION_STAMP}
rel_type: ${RELEASE_TYPE}
profile: ${STAGE3_PROFILE}
snapshot_treeish: ${PORTAGE_SNAPSHOT}
source_subpath: ${VERSION_STAMP}/stage2-${ARCH}-${RELEASE_TYPE}.tar.gz
compression_mode: gzip
